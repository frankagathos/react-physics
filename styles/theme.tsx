import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#0f3527',
    },
    secondary: {
      main: '#318DAD',
    },
  },
})

export default theme
